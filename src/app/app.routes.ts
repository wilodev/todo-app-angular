import { Routes } from '@angular/router';
import { TodoPageComponent } from './modules/todo-page/components/todo-page/todo-page.component';

export const routes: Routes = [{ path: '', component: TodoPageComponent }];
